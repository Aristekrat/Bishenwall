'use strict';

$(document).ready(function () {

/*
function notEmpty(reviewedElement) {
   !reviewedElement ? false : true; 
}

$(".bwMainForm").submit(function(event) {
    event.preventDefault(); 
    var commentTitle = $("input[name='comment[title]']").val();
    console.log(commentTitle);  
    if(notEmpty(commentTitle) === false) {
            $(commentTitle).append("<p>Please enter a comment title</p>");
            console.log("Kinda working"); 
        } else {
            console.log("Not at all working"); 
        }
    }); */
});
